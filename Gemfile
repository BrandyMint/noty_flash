source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in noty_flash.gemspec
gemspec

source 'https://rails-assets.org' do
  gem 'rails-assets-noty' # используется для флешек в application
end

